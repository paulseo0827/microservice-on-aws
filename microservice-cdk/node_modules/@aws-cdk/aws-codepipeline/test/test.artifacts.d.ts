import { Test } from 'nodeunit';
declare const _default: {
    'Artifacts in CodePipeline': {
        'cannot be created with an empty name'(test: Test): void;
        'without a name, when used as an input without being used as an output first - should fail validation'(test: Test): void;
        'with a name, when used as an input without being used as an output first - should fail validation'(test: Test): void;
        'without a name, when used as an output multiple times - should fail validation'(test: Test): void;
        "an Action's output can be used as input for an Action in the same Stage with a higher runOrder"(test: Test): void;
        'violation of runOrder constraints is detected and reported'(test: Test): void;
        'without a name, sanitize the auto stage-action derived name'(test: Test): void;
    };
};
export = _default;
