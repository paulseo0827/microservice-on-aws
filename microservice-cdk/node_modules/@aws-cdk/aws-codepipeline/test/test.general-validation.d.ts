import { Test } from 'nodeunit';
declare const _default: {
    'name validation'(test: Test): void;
    'Stage validation': {
        'should fail if Stage has no Actions'(test: Test): void;
    };
    'Pipeline validation': {
        'should fail if Pipeline has no Stages'(test: Test): void;
        'should fail if Pipeline has a Source Action in a non-first Stage'(test: Test): void;
    };
};
export = _default;
