import { Test } from 'nodeunit';
declare const _default: {
    'Pipeline Variables': {
        'uses the passed namespace when its passed when constructing the Action'(test: Test): void;
        'allows using the variable in the configuration of a different action'(test: Test): void;
        'fails when trying add an action using variables with an empty string for the namespace to a pipeline'(test: Test): void;
        'can use global variables'(test: Test): void;
    };
};
export = _default;
